# tt-landing

Static site for `tastingandtoasting.com` (Vercel).

`public/.well-known/apple-app-site-association` enables iOS Universal Links for `/onboarding/*` (winery magic link).

The `appID` matches the **production** iOS bundle identifier: `Tasting.And.Toasting` (team `4N3W42SWLG`).
